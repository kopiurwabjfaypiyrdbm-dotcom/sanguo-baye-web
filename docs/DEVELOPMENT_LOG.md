# 开发日志

## 2026-07-28：战略地图美术资源初版

### 已完成

- 生成战略主地图底图：山脉、河流、平原与道路采用写实水墨风格，保留大面积可叠加区域。
- 生成独立城池标记：铜金、黛黑与朱砂配色的城门印章造型，已转换为透明 RGBA PNG。
- 地图底图不包含城池、势力、文字或 UI；城池归属颜色、选中描边、名称和状态徽记保留给代码层处理。
- 新增地图资源清单：`assets/manifests/map-art.json`。

### 资源位置

- `assets/production/map/strategic-map-background-v1.png`
- `assets/production/map/city-marker-base-v1.png`
- `assets/source/map/`：保留生成源图和城池标记抠色前的 chroma-key 源图。

### 当前状态

- 本次资源暂未接入 Phaser 地图代码，等待协作者接入可编辑地图和城池数据层。
- 当前提交：`3cf3591 art: add strategic map and city marker assets`。

## 2026-07-28：立体城池与势力旗帜补充

- 新增完整立体城池模型 `assets/production/map/city-model-v2.png`，包含外城墙、角楼、城门楼、内城建筑、道路、桥梁和护城河。
- 新增独立势力旗帜 `assets/production/map/faction-flag-base-v1.png`，保留中性朱砂底色，后续由代码按势力替换颜色。
- 两项素材均已处理为透明 RGBA PNG，未覆盖上一版平面城池标记，便于后续比较和回退。
- 资源信息已追加到 `assets/manifests/map-art.json`。

### 旗帜 v2 调整

- 旗面改为宽幅金边燕尾旗，缩短旗杆并降低旗杆视觉权重。
- 旗面中央保留无图案留白，后续可由 React/Phaser 叠加单字势力简称。
- 新增 `assets/production/map/faction-flag-base-v2.png`，旧版 `v1` 继续保留作为备选。

### 旗帜 v3：参考三国古风军旗

- 按用户提供的参考样式重做：黑色外旗、金色中央旗面、朱红边框、顶部横梁和多条破风飘带。
- 中央金色旗面保持纯净留白，不内置势力文字，后续可叠加一个势力简称。
- 新增 `assets/production/map/faction-flag-reference-v3.png`，`v1` 与 `v2` 继续保留作为备选。

## 2026-07-28：青灰方形城池模型

- 城池主色改为石板青灰、冷灰屋瓦和低饱和蓝绿色植被，减少原 v2 的暖金属感。
- 城池布局改为规整方形城墙、四角城楼、中央内城、十字街道、护城河与正面吊桥。
- 新增 `assets/production/map/city-model-slate-v3.png`，旧版城池模型继续保留作为备选。
- 资源信息已追加到 `assets/manifests/map-art.json`。

## 2026-07-28：战略地图美术接入试玩版

- `src/game/MapScene.ts` 已接入战略地图底图、青灰方形城池模型和参考风格势力旗帜。
- 原有地图拖动、滚轮缩放、道路连接、城池点击、城名和选中状态继续保留。
- 势力颜色通过旗帜中央的独立色块显示，保留黑色外旗、金边和可叠加势力简称的旗面层。
- 地图美术采用场景创建后的原生图片加载并注册为 Phaser 纹理，同时保留矢量回退画面，避免异步资源加载影响试玩入口。
- 战略地图城池缩小为首都 68、普通城 54，旗帜和名称同步缩小；取消悬停缩放，改为金色描边高亮。
- 旗帜锚点调整到城池中心线，城池模型位于旗杆底部下层，形成旗帜插入城池中央的层次效果。
- 所有城池统一缩放为 48，取消首都尺寸差异；旗帜同步调整为 43，继续使用内城中心落点。
- 生产构建通过，并在本地浏览器完成从标题页、剧本选择、君主选择到战略地图的试玩检查。
## 2026-08-02：Godot 4.7.1 迁移技术样片

### 已完成

- 保留现有 Web 产品路径，在顶层新增独立 `godot/`；主场景使用原生 `Node2D`、`Camera2D`、`Control`、Tween 和程序化绘制呈现时期 1 的完整 38 城、54 条互惠道路。
- 领域状态、LCG、校验和开垦命令使用不挂场景树的纯 GDScript 对象；语言无关 JSON fixture 把 TypeScript 的 seed 48641 → 373686124、农业 +63、金钱 -50、体力 -8 和日志 receipt 与 Godot 逐字段对照。
- 加入最小 JSON 存档；MuMu 上实测开垦、保存、强制停止进程、重新启动并读取后状态一致。
- 使用指定 Godot 4.7.1 导出 Android Debug APK；包内没有 Web/JS/WASM/`.lib` 或受限媒体，manifest 不申请网络权限。
- 迁移决策、结构化数据发布边界、设备证据和继续迁移门槛记录在 `docs/migration/godot-spike-report.md`。

### 验证状态

- Godot 领域/fixture/存档测试 140 条断言通过；表现输入烟测 6 条断言通过；编辑器导入、配置主场景启动和最终 APK 导出退出码均为 0。
- MuMu Android 15/API 35 实测默认横屏、1280×720、844×390，覆盖触摸点选、空间城池卡片、确定性开垦、放大后拖动和跨进程存读；实体 Android 双指手感与安全区仍待人工验收。
- 最终 APK 为 57,172,135 字节，SHA-256 `150AA738F4F2FFF90F7E58892F3A84A45C573567D91BF2959FD7AA9B2E5452D9`，v2/v3 调试签名验证通过。
- `npm run check` 通过：51 个固定 C 参考文件校验、360 项测试通过、4 项条件测试跳过，TypeScript、Vite 与 PWA 构建成功。

## 2026-08-02：城池命令默认执行者与库存反馈

### 已完成

- 打开具体城池命令时，默认执行者会优先选择尚未行动、体力达到该命令要求且身份合法的武将；奖赏、宴请、太守和流放等目标型命令也会避开显然无效的人选。
- 没有完全可执行人选时，优先回退到尚未行动的驻城武将，再回退到原始驻城顺序；用户手动改选后不会被自动选择逻辑覆盖。
- 命令摘要中的金钱、粮草、后备兵和体力统一显示为“本次消耗 / 当前库存”，例如“金钱 50 / 城中 187”“体力 8 / 马玩 100”；原有红色禁用原因继续解释资源不足或其他阻塞。
- 城池节点一级快捷菜单补充城中金钱，与粮草和总兵力并列，常用基础状态不再必须进入详情页查看。
- 本轮只调整默认选择和信息呈现，没有改动命令规则、成本、随机数、存档或 AI。

### 验证状态

- 浏览器实测同一存档中马腾已行动、马玩待命：开垦和招商均默认选中马玩，命令按钮直接可用，库存数字与天水实际状态一致。
- 手动改选杨秋后选择保持稳定，收益范围与体力库存同步刷新；返回一级菜单可见“金 187 · 粮 505 · 兵 800”。
- `npm run check` 通过：51 个固定 C 参考文件校验、358 项测试通过、4 项本地参考测试跳过，TypeScript、Vite 与 PWA 构建成功。

## 2026-08-02：城池快速命令保持节点锚定

### 已完成

- 城池根菜单、分类菜单与快速命令编辑器共用地图节点锚点；开垦、治理、出巡、奖赏等快速操作不再突然跳转为底部大面板。
- 快速编辑器改为节点左右侧的窄竖卡，按实际内容自适应高度；优先贴靠节点右侧，空间不足时自动换到左侧，并保留指向当前城池的箭头、相同返回入口和轻量展开动画。
- 快速编辑器不再触发战役壳层的“命令工作区”布局，地图、画布、情报与底部导航在分类菜单切换到具体命令时保持原尺寸，避免 Phaser 因容器重排重算镜头而出现位移或缩放抖动。
- 交易、输送、道具、俘虏、出征和谋略等参数密集命令继续使用大型工作区，避免为了视觉统一压缩必要字段。
- 命令执行和返回规则不变：快速命令完成或返回后恢复原分类菜单，复杂工作区仍沿用既有返回链。

### 验证状态

- 1280×720 浏览器几何实测：从天水内政分类进入治理前后，地图与画布均保持 `1256×590` 且坐标不变；治理编辑器为节点右侧 `340×293` 竖卡，底部导航持续显示，页面无滚动。
- 交易命令仍以 900×420 复杂工作区展示，未误套快速命令布局。
- `npm run check` 通过：51 个固定 C 参考文件校验、358 项测试通过、4 项本地参考测试跳过，TypeScript、Vite 与 PWA 构建成功。
- Debug APK 构建、覆盖安装与 MuMu 横屏实测通过；天水节点在内政分类与治理竖卡切换前后位置、缩放一致，底部六项导航持续可见，Android 系统返回仍可回到同一节点的内政分类菜单。

## 2026-08-02：移动端单层城池命令编辑器

### 已完成

- 保留“城市节点 → 分类 → 具体命令”的地图锚定层级；具体命令改为单一聚焦编辑器，普通命令在同层展示执行者、参数、预期效果、成本、行动消耗和禁用原因后直接提交，不再打开通用复核弹窗。
- 仅掠夺、流放、非君主装备没收、俘虏流放与处斩等不可逆操作保留一次简短危险确认；取消会回到原命令编辑器且保留当前选择。
- 命令目录统一记录快速/复杂编辑器密度和固定危险属性；长表单可在内容区触控滚动，命令标题、返回入口和底部主操作保持可见，全局战役底栏在编辑器期间让位。
- 分类菜单会提前禁用并说明无人才、无俘虏、无道具、无道路目标、无敌邻、无侦察/谋略目标和经典规则自动太守等已知结构性不可用状态，避免进入无解释的死路。
- 21 个现有城池命令仍由原有核心回调执行；本轮没有修改规则、成本、随机数、存档或日志路径。
- Vitest 固定为单工作线程运行；检查时间略增，但避免并行测试池与开发服务器或 Android 构建争抢 CPU，并消除本机测试工作进程异常退出。

### 验证状态

- `npm run check` 通过：51 个固定 C 参考文件校验、358 项测试通过、4 项本地参考测试跳过，TypeScript、Vite 生产构建与 PWA 产物生成成功。
- 844×390 浏览器实测开垦普通命令直接完成且没有第四层复核；效果、成本、禁用原因和固定主按钮无互相遮挡，执行后回到原内政分类。
- 掠夺实测会因已行动武将显示即时禁用原因，切换可行动武将后只出现一次危险警告，取消可回到保留选择的命令编辑器。
- 输送在 667×375、844×390 和 1462×822 下均无页面滚动或全局底栏重叠；内部滑动后货物字段可达且底部主操作保持不动。
- Escape 返回链实测为“危险确认 → 命令编辑器 → 分类 → 城市根菜单 → 地图”，回到地图后全局底栏恢复。
- Debug APK 已完成构建、安装并在 MuMu 横屏模拟器实测；命令编辑器显示、内部触控滑动、固定主操作及 Android 系统返回链均通过。
- 验收结束后关闭 5180 开发服务；空闲采样中所有 Node 进程 2 秒 CPU 增量均为 0，未发现由命令编辑器引入的持续循环。

## 2026-08-02：移动端地图主场景与全局功能入口

### 已完成

- 地图固定为战略主页，关闭城池或全局功能面板后直接返回地图，不再在底栏重复提供“地图”入口。
- 底栏重组为“情报、城池、人物、宝物、委任、结束本月”：前三者提供本月待办和全局资产索引，宝物与委任先建立可持续扩展的产品入口。
- 地图点城池会在节点旁打开非模态快捷菜单，不联动激活底栏“城池”；内政、人事、军事、谋略在原位置替换为分类子菜单，选中具体命令后才打开只包含该命令参数的浅操作条。返回键按“单命令操作条 → 分类子菜单 → 城池快捷菜单 → 地图”逐层收起。
- 己方城池增加常驻金色领地环与旗标，当前选中城市使用独立的更大高亮环，势力归属与选中状态不再混用。
- 情报页汇总本月提醒、待命武将、在途命令和月度消息；带城池位置的消息可直接跳转对应城池。
- 城池、人物、宝物和委任分别提供全局索引；委任入口明确标记为后续自动经营规则的扩展位，当前不伪装成已经执行自动策略。

### 验证状态

- `npm run check` 通过：51 个固定 C 参考文件校验、356 项测试通过、4 项本地参考测试跳过，TypeScript 与生产构建成功。
- 浏览器验证了六入口底栏、情报/城池/人物/宝物/委任页面、城池节点快捷菜单、原位分类子菜单、城池详情页和单命令操作条；全局页面、详情页与命令层级互斥。
- Debug APK 已安装到 MuMu Android 模拟器并完成横屏触控验收：新建战役、时期/君主选择、地图主场景、己方城池标识、城池节点快捷菜单、分类子菜单、单命令操作条、情报入口与系统返回键均可用。

## 2026-08-01：PWA 与 Android 横屏应用壳

### 已完成

- 加入 PWA 安装清单、192/512/maskable 图标、确认式 Service Worker 更新、离线就绪/离线状态和安装提示。
- 核心游戏壳纳入预缓存；入口视频和超过 2 MiB 的选时期背景改为按需 Cache First，避免首次安装被大型装饰媒体阻塞。
- 加入 Capacitor 8 Android 工程，临时包名 `com.sumo91.sanguobaye.debug`，Web 产物从 `dist` 同步。
- Android 原生层锁定 `sensorLandscape`，启用沉浸式系统栏、刘海短边布局、深色启动主题和项目自有应用图标。
- 系统返回键会优先关闭战术边缘层、命令确认、月末、目录和城池抽屉，再逐层返回标题；标题页返回会最小化应用。
- 增加 Android 图标/启动图生成脚本、跨平台 Debug Gradle 构建脚本与 `android:doctor`、`android:sync`、`android:apk` 等命令。

### 验证状态

- `npm run check` 通过：51 个固定 C 参考文件校验、356 项测试通过、4 项本地参考测试跳过，TypeScript 与生产构建成功。
- PWA 生成 22 个去重后预缓存条目，约 4.5 MiB；大型入口媒体未进入预缓存。
- 844×390 生产预览实测离线就绪、重载接管、无页面溢出、新建战役与四剧本入口；控制台无警告或错误。
- `npm audit --omit=dev --audit-level=high` 报告生产依赖 0 个已知漏洞。PWA/Capacitor CLI 的开发构建依赖仍有上游审计告警，不进入浏览器或 APK 运行时。
- `npm run android:assets` 和 `npm run android:sync` 通过；Capacitor Doctor 确认 Android、CLI 与 Core 均为 8.5.0。
- Android Studio 2026.1、SDK Platform 36、Build Tools 35 与 Temurin JDK 21 环境已完成原生编译；修正 `MainActivity.onResume()` 可见性，并让构建脚本拒绝 Gradle 8.14 不支持的 JBR 25、自动选择兼容 JDK。Debug APK 的 v2 调试签名、包名、API 24/36 与 `sensorLandscape` 声明已核验；MuMu 模拟器安装与触控验收通过，物理手机验收仍待进行。

### 许可证边界

- 应用图标是项目内新建的代码原生矢量图，不含原版二进制、美术或字体。
- 标题视频仍保留 `integration-pending` 来源状态，只作可选装饰资源；不影响离线核心游戏壳。

## 2026-07-28：入口视觉资源接入

### 已完成

- 完成标题页与剧本选择页的入口美术资源整理，并纳入 `assets/production/entry/`。
- 标题页接入循环播放的入口背景视频，保留静态背景作为加载失败时的后备图。
- 接入“三国霸业”独立透明字标、四个剧本背景图和剧本选择页背景底图。
- 保留分层结构：场景底图、字标、DOM 文字、CSS 边框和透明装饰纹样彼此独立。
- 剧本选择页改为“选择剧本”标题，移除多余的绿色半透明框、云纹横饰、印章框和时期编号。
- 调整返回按钮、剧本卡片尺寸与间距，补充金属质感的选中描边和悬停状态。
- 新增入口资源清单与图层清单：
  - `assets/manifests/entry-art.json`
  - `assets/manifests/entry-layers.json`

### 代码改动

- `src/ui/CampaignSetup.tsx`：接入视频背景、剧本选择背景和分层资源引用。
- `src/styles.css`：更新标题页、剧本选择页布局、按钮、卡片选中态和响应式间距。

### 验证状态

- `git diff --check` 通过。
- Vite 生产构建通过。
- 当前提交：`3302ea7 feat: add campaign entry visuals`。
- 原版二进制、美术和字体仍未进入仓库；本次资源为项目自有或已整理的 Web 发布资源。

### 下一步

- 继续由协作者将入口视觉层接入更完整的游戏流程。
- 根据试玩反馈调整移动端适配、资源加载策略和标题页动效。
- 暂不改动地图与核心玩法规则，保持当前战略玩法和战术 Alpha 进度稳定。

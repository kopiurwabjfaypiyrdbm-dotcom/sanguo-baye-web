# Godot 迁移 Mission 报告

每个完成的实施 Mission 在此留下简短、可复验的结果报告。MB01 的既有报告保留在 `docs/migration/godot-spike-report.md`；后续默认使用 `MBNN-<stable-slug>.md`。

报告应记录结果、提交、实际验证命令、设备/视觉证据、审查与修复、剩余不确定性，以及下一 Mission 的选择依据。报告不覆盖 `godot-program-state.json` 的当前状态。
